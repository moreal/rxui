# Architecture decisions

| ADR | Status | Decision | Date |
|---|---|---|---|
| [0001](docs/adr/0001-lean-host-language.md) | Accepted | Lean 4 is the host language | 2026-08-19 |
| [0002](docs/adr/0002-staged-reactive-core.md) | Accepted | A typed staged DSL is the semantic boundary | 2026-08-19 |
| [0003](docs/adr/0003-custom-reactive-ir-js-backend.md) | Accepted | Use a custom Reactive IR to JavaScript backend | 2026-08-19 |
| [0004](docs/adr/0004-direct-dom-static-shape.md) | Accepted | Use direct DOM updates for static shape | 2026-08-19 |
| [0005](docs/adr/0005-trust-boundary.md) | Accepted | Limit proof claims and document the remaining TCB | 2026-08-19 |
| [0006](docs/adr/0006-scoped-component-jsx-syntax.md) | Accepted | Use scoped balanced component and JSX syntax | 2026-08-19 |
| [0007](docs/adr/0007-atomic-versioned-output.md) | Accepted | Publish versioned bundles through an atomic pointer | 2026-08-19 |
| [0008](docs/adr/0008-runtime-abi-v2.md) | Accepted | Bump the internal runtime ABI for M5 transactions | 2026-08-19 |
| [0009](docs/adr/0009-m5-benchmark-smoke-scope.md) | Accepted | Treat the M5 graph runner as a pre-benchmark smoke harness | 2026-08-19 |
| [0010](docs/adr/0010-runtime-abi-v3-dependent-values.md) | Accepted | Bump the internal runtime ABI for dependent values and typed event payloads | 2026-08-19 |
| [0011](docs/adr/0011-fin-literal-normalization.md) | Accepted | Exclude modulo-normalized `Fin` literals from public selection construction | 2026-08-19 |
| [0012](docs/adr/0012-poc-dogfood-set.md) | Accepted | Align the PoC dogfood set with M0–M6 and defer Temperature Converter to M7 | 2026-08-19 |
| [0013](docs/adr/0013-runtime-abi-v4-form-events.md) | Accepted | Bump the internal runtime ABI for typed form properties and payload events | 2026-08-19 |
| [0014](docs/adr/0014-runtime-abi-v5-dynamic-regions.md) | Accepted | Bump the internal runtime ABI for local dynamic regions and delegated row events | 2026-08-19 |
| [0015](docs/adr/0015-runtime-abi-v6-owned-effects.md) | Accepted | Bump the internal runtime ABI for owned commands and explicit foreign ports | 2026-08-20 |
| [0016](docs/adr/0016-runtime-abi-v7-structural-deltas.md) | Accepted | Bump the internal runtime ABI for checked structural keyed deltas | 2026-08-21 |
| [0017](docs/adr/0017-structural-delta-remains-opt-in.md) | Accepted | Keep structural delta an opt-in experimental library | 2026-08-21 |
| [0018](docs/adr/0018-runtime-abi-v8-template-clone.md) | Accepted | Bump the internal runtime ABI for template cloning and minimal keyed placement | 2026-08-22 |
| [0019](docs/adr/0019-runtime-abi-v9-owned-parent-rebuild.md) | Accepted | Bump the internal runtime ABI for owned-parent rebuilds, property keys, and a separate delta host | 2026-08-22 |
| [0020](docs/adr/0020-runtime-abi-v10-keyed-context-and-update-at.md) | Accepted | Bump the internal runtime ABI for keyed-region context forwarding and single-row updates | 2026-08-22 |
| [0021](docs/adr/0021-runtime-abi-v11-form-events-host.md) | Accepted | Bump the internal runtime ABI for a separate form-event host | 2026-08-23 |
| [0022](docs/adr/0022-runtime-abi-v12-disposer-in-dom-host.md) | Accepted | Bump the internal runtime ABI to ship the disposer in the DOM host | 2026-08-23 |
| [0023](docs/adr/0023-flattened-benchmark-module.md) | Accepted | Flatten the js-framework-benchmark application into one module at build time | 2026-08-23 |
| [0024](docs/adr/0024-compacted-benchmark-module.md) | Accepted | Compact the flattened js-framework-benchmark module with a Lean tokenizer | 2026-08-23 |
| [0025](docs/adr/0025-precedence-aware-javascript-printer.md) | Accepted | Print JavaScript expressions by operator precedence and drop effect-only returns | 2026-08-23 |
| [0026](docs/adr/0026-runtime-abi-v13-keyed-swap-and-remove.md) | Accepted | Bump the internal runtime ABI for keyed-region swap and single-row removal | 2026-08-23 |
| [0027](docs/adr/0027-monotone-keys-without-index.md) | Accepted | Validate monotone keys without an index in the keyed region | 2026-08-23 |
| [0028](docs/adr/0028-runtime-abi-v14-next-text.md) | Accepted | Bump the internal runtime ABI for text-slot traversal in the DOM host | 2026-08-23 |
| [0029](docs/adr/0029-safe-integer-benchmark-ids.md) | Accepted | Represent js-framework-benchmark row ids as safe integers | 2026-08-23 |
| [0030](docs/adr/0030-runtime-abi-v15-structural-delegation.md) | Accepted | Bump the internal runtime ABI for structural row-click delegation | 2026-08-23 |
| [0031](docs/adr/0031-pruned-benchmark-module.md) | Accepted | Drop unreachable host declarations from the flattened js-framework-benchmark module | 2026-08-23 |
| [0032](docs/adr/0032-structural-benchmark-buttons.md) | Accepted | Resolve the js-framework-benchmark buttons by structure | 2026-08-23 |
| [0033](docs/adr/0033-rx-expression-surface.md) | Accepted | Stage ordinary expression syntax with rx% | 2026-08-24 |
| [0034](docs/adr/0034-dual-target-jsx-surface.md) | Accepted | Lower one JSX surface into typed views and the logical region model | 2026-08-24 |
| [0035](docs/adr/0035-non-reserved-surface-keywords.md) | Accepted | Parse surface keywords as plain identifiers | 2026-08-24 |
| [0036](docs/adr/0036-component-item-sugar.md) | Accepted | Sugar component items and bind events by reference | 2026-08-24 |
| [0037](docs/adr/0037-typed-event-payloads-in-component-backend.md) | Accepted | Lower typed event payloads through the generic component backend | 2026-08-24 |
| [0038](docs/adr/0038-controlled-inputs-in-component-backend.md) | Accepted | Reflect controlled inputs through the generic component backend | 2026-08-25 |
| [0039](docs/adr/0039-static-child-component-composition.md) | Accepted | Compose stateful child components by static module import | 2026-08-25 |
| [0040](docs/adr/0040-app-ir-for-dynamic-region-events.md) | Accepted | Sketch an App IR to generalize Backend.Todo's event lowering | 2026-08-25 |
| [0041](docs/adr/0041-sealed-row-binders-for-keyed-regions.md) | Accepted | Bind keyed region rows through sealed row binders in the generic backend | 2026-08-25 |
| [0042](docs/adr/0042-immutable-props-across-the-mount-abi.md) | Accepted | Pass immutable child props as a positional mount ABI array | 2026-08-25 |
| [0043](docs/adr/0043-sealed-row-expressions-and-row-updates.md) | Accepted | Update keyed rows through sealed row expressions and updateAt | 2026-08-25 |
| [0044](docs/adr/0044-row-scoped-class-selection.md) | Accepted | Select row classes through a sealed row-scoped predicate | 2026-08-25 |
| [0045](docs/adr/0045-filter-region-as-static-view-selection.md) | Accepted | Express TodoMVC's filter row as static view with state-scoped selection | 2026-08-25 |
| [0046](docs/adr/0046-typed-row-event-payloads.md) | Accepted | Deliver typed row event payloads through structural delegation | 2026-08-25 |
| [0047](docs/adr/0047-sealed-row-branch-structure.md) | Accepted | Select conditional row structure through a sealed row branch cell | 2026-08-25 |
| [0048](docs/adr/0048-row-focus-vocabulary.md) | Accepted | Focus fresh row branch inputs through an explicit marker and host export | 2026-08-25 |
| [0049](docs/adr/0049-row-dblclick-and-checked-delegation.md) | Accepted | Extend the delegated row kinds with dblclick and checkbox toggles | 2026-08-26 |
| [0050](docs/adr/0050-row-aggregates-and-region-broadcasts.md) | Accepted | Count, broadcast into, and filter keyed rows through the region record | 2026-08-26 |
| [0051](docs/adr/0051-sealed-region-filter-views.md) | Accepted | Select keyed row visibility through a sealed filter table | 2026-08-27 |
| [0052](docs/adr/0052-sealed-row-key-branching.md) | Accepted | Branch keydown row events on sealed key literals | 2026-08-27 |
| [0053](docs/adr/0053-sealed-row-field-guards.md) | Accepted | Guard row stages on sealed field equality for remove-or-commit | 2026-08-27 |
| [0054](docs/adr/0054-sealed-row-expression-trim.md) | Accepted | Trim row expressions through one sealed unary for the commit contract | 2026-08-28 |
| [0055](docs/adr/0055-component-scope-add-path.md) | Accepted | Close the component-scope add path with the RxExpr trim and the sealed skip-if event guard | 2026-08-28 |
| [0056](docs/adr/0056-component-scope-key-branching.md) | Accepted | Close the Enter-to-add gap with the sealed key-branched component event | 2026-08-28 |
| [0057](docs/adr/0057-trimmed-attribute-selection.md) | Accepted | Extend the attribute-selection subject with the sealed trim unary for the Add affordance | 2026-08-28 |
| [0058](docs/adr/0058-empty-region-visibility.md) | Accepted | Close the hide-when-empty parity with the sealed region-subject hidden selection | 2026-08-28 |
| [0059](docs/adr/0059-predicate-count-visibility.md) | Accepted | Hide the clear-completed affordance through the sealed predicate-count hidden subject | 2026-08-28 |
| [0060](docs/adr/0060-region-checked-reflection.md) | Accepted | Reflect the sealed region-count boolean into the toggle-all checkbox's checked property | 2026-08-28 |
| [0061](docs/adr/0061-payload-broadcast.md) | Accepted | Flow the delegated checked payload into the region broadcast to close toggle-all | 2026-08-28 |
| [0062](docs/adr/0062-count-label-selection.md) | Accepted | Select between two static strings from a region count against the one literal — items-left singular/plural | 2026-08-28 |
| [0063](docs/adr/0063-freeze-boundary-routing-persistence.md) | Accepted | Take the parity axis: routing and persistence host exports as one ABI 17 round under the pruning-sealed freeze boundary; defer the field-predicate unification | 2026-08-28 |
| [0064](docs/adr/0064-field-predicate-unification.md) | Accepted | Unify the single-field-literal equality behind one sealed FieldPredicate, one shared comparison builder, and one bounds rule — byte-neutral under the freeze | 2026-08-28 |
| [0065](docs/adr/0065-affordance-contract-alignment.md) | Accepted | Reject the affordance-contract alignment check — affordance predicates are presentation policy and the dispatch layer stays the one contract | 2026-08-28 |
| [0066](docs/adr/0066-child-instrumentation-reachability.md) | Accepted | Reach child instrumentation through the parent disposer's `children` array — republish child mount returns in generated code; reject counter merging and host accessors | 2026-08-28 |
| [0067](docs/adr/0067-transitive-child-composition.md) | Accepted | Transitive child composition needs no new vocabulary — the ADR-0066 republication applies per level and `children[i].children[j]` is the composed surface, pinned by the two-level NestLab | 2026-08-28 |
| [0068](docs/adr/0068-parent-prop-forwarding.md) | Accepted | A child prop value may be exactly one declared immutable prop of the parent — `label={title}` lowers to a `ChildProp.forward` index and mounts as `props[i]`, still a mount-time constant; everything else stays rejected | 2026-08-28 |
| [0069](docs/adr/0069-transitive-prop-reforwarding.md) | Accepted | Transitive prop re-forwarding needs no new vocabulary — the forwarding rewrite never asks where the parent's value came from, so a chain is per-level ADR-0068 links, pinned by the three-level NestLab | 2026-08-28 |
| [0070](docs/adr/0070-fanout-prop-reforwarding.md) | Accepted | Fan-out prop re-forwarding needs no new vocabulary — the child table, aliased imports, and `children` republication all scale by declaration order, so two forwards of one received prop are independent ADR-0068 links, pinned by the `Chip` sibling leaf | 2026-08-28 |
| [0071](docs/adr/0071-repeated-child-composition.md) | Accepted | Repeated composition of the same child needs no new vocabulary — the table dedups by name (one aliased import) while every reference keeps its own `ChildProp` list and `child_off_{n}`, pinned by the second `Chip` instance mixing a forward with a literal | 2026-08-28 |
| [0072](docs/adr/0072-no-child-composition-in-region-rows.md) | Accepted | Sealed row templates do not compose child components — a capitalized head in a region row is a lifecycle-contract conflict, rejected at elaboration with the dedicated `LRX-ELAB-131` instead of the misleading tag-whitelist `LRX-VIEW-007` | 2026-08-28 |
| [0073](docs/adr/0073-misshapen-child-reference-diagnostic.md) | Accepted | A spec'd capitalized head whose shape leaves the child-reference contract is rejected at the typed-application fallback with `LRX-ELAB-132` — a term-elab guard where the spec is visible, replacing `Unknown identifier`; macro-time rejection is impossible without breaking the ADR-0039 spec-less application | 2026-08-28 |
| [0074](docs/adr/0074-spec-less-head-children-rejection.md) | Accepted | Non-empty children on a spec-less capitalized head are rejected with `LRX-ELAB-133` instead of vanishing silently — the ADR-0039 ordinary application consumes attributes only, so the children were dead syntax; the logical reference view routes through the same guard (spec'd head → `LRX-ELAB-132`, replacing the raw unknown-identifier), closing ADR-0073 OQ1/OQ2 | 2026-08-28 |
| [0075](docs/adr/0075-per-row-child-composition.md) | Accepted | Seal per-row child composition on the narrowest coherent surface — one self-closing child per row template, props as row-mount constants (literals or never-written row fields, `LRX-VIEW-045` otherwise), no static id in the child template (`LRX-ELAB-135`), mounted at its cell and disposed through the row dispose callback, republished on the live `children` inventory; everything else stays `LRX-ELAB-131`, closing ADR-0072 OQ1 with no host change | 2026-08-28 |
| [0076](docs/adr/0076-row-child-region-feature-coexistence.md) | Accepted | Row-child composition coexists with counts, filters, and persistence — the surveyed axes (slot arithmetic, hydration child context, filter row-root navigation, row-table-scoped subjects) are coherent, so the round gates the combination instead of sealing anything: Mix Lab puts all four features on one region and the artifact gate pins the widest 9-slot record layout and the `regions[0][8]` child context; no code change, ABI stays 17 | 2026-08-28 |
| [0077](docs/adr/0077-broadcast-and-shared-inventory-closure.md) | Accepted | Broadcasts retain row children and child-composing regions share one inventory — a broadcast retains every key so the reconcile never remounts a child, and the record construction hands every child-composing region the same `childInventory` identifier at its own `regionChildSlot`; Mix Lab gains a `markAllDone` broadcast and a bare `pins` region (the first two-region component), gates pin the two-record literal, both `indexOf` dispose splices, chronological cross-region interleaving, and splice isolation, plus a `RowChildBroadcastField` witness for the LRX-VIEW-045 broadcast-writer arm; no code change, ABI stays 17 | 2026-08-28 |
| [0078](docs/adr/0078-multi-region-feature-distribution.md) | Accepted | Region features distribute per region, and a component persists one key per region — the survey found per-region sweep identifiers, feature slots, count records, document-order attr labels, and cross-region chained events all coherent by construction, but `validatePersists` capped the whole component at one persist item while the backend was already index-generalized; the cap becomes one item per region with keys distinct across the component (both violations `LRX-TYPE-118`, the invariant is one writer per key), and Mix Lab's `pins` takes its own count, emptiness selection, persist key, and a chained `append pins … then remove crew …` event, with the artifact gate pinning crew's container and pins' inventory at the same slot number 7; ABI stays 17 | 2026-08-28 |
| [0079](docs/adr/0079-multi-filter-distribution.md) | Accepted | Filters distribute per region, including two driven by one state field — the survey found the scan identifiers, container slots (`5 + counts?2`), wake guards (`region_touched_{i} || changed[field]`), declaration-order sweep, and the region-keyed `validateFilters` all per-region by construction, with no scaffolding cap behind them, so the round gates the combination in a new Twin Lab (three filtered regions, twins over one field with inverted tables, a control on its own field, and a `stir` event mixing a region touch with a shared filter change) rather than in Mix Lab, whose evidence depends on `pins` staying unfiltered; `FilterRegionTwice` witnesses that two filters over one region stay `LRX-TYPE-113`; no code change, ABI stays 17 | 2026-08-28 |
| [0080](docs/adr/0080-route-over-shared-filter-field.md) | Accepted | A route seals onto the *union* of every ADR-0051 filter table over its field, not the first-declared one — `validateRoutes` resolved the table with `spec.filters.find?`, so with two filtered regions the sealed literal set (and therefore which `LRX-TYPE-117` rejections fired) read declaration order, while codegen never consults the filter table for a route at all, each region's arm chain falls through to show-all on a literal it does not name, one `hashchange` wakes every sweep guarded on the field, and `writeHash` rides the field's changed flag once per commit; Twin Lab gains `route mode` with a `#/mixed` arm legal only through `right`'s table, plus a `row right toggle` pairing a pending-row drain with a filter sweep at region index 1, and `RouteLiteralOutsideFilterUnion` witnesses the rejection now naming both tables; validation-only, ABI stays 17 | 2026-08-28 |
| [0081](docs/adr/0081-one-hash-one-writer.md) | Accepted | The `spec.routes.size ≤ 1` cap is a principled constraint, not scaffolding, and is restated as *one hash, one writer*: two route items generate cleanly — arms, dispatch, seed, and write are all indexed by `routeIndex` — but `location.hash` is one string with one writer while a route table is a *total* function from that string, so the two `if (changed[field])` write blocks race inside one commit and one `hashchange` wakes both dispatches with the second reading the hash the first synchronously rewrote; the unknown-hash fallback then resets the loser's field, observed as a seven-commit cascade returning both fields to their declared defaults from one click. A lift would need disjoint sub-namespaces with partial tables (changing every route's fallback contract) or one table over the tuple of routed fields (the cap with a wider field). Five fixtures pin the unwitnessed `LRX-TYPE-117` branches by message, the empty-arm branch is witnessed at spec level because `sepBy1` makes it unwritable, and the missing-`String`-initial branch is documented dead; Twin Lab's `persist right` closes ADR-0080 OQ1 — the persistence sweep rides `region_touched_1` alone and the hash write rides `changed[mode]`, and persistence adds no region-record slot | 2026-08-28 |
| [0082](docs/adr/0082-drain-scoped-filter-wake.md) | Accepted | A storage key is a namespace and a region touch is two events — `storageSet("")` was observed in Chromium to be a working, enumerable localStorage key, and hydration is fail-closed only against a wrong arity (a same-arity foreign value hydrates as this region's own rows and is re-persisted as such), so the empty key is the one value that breaks *one writer per key* between components where no validator can look and stays rejected on that ground (`PersistEmptyKey` witnesses the fourth `validatePersists` branch, pinned on its message); and ADR-0081 OQ2 closes by narrowing the filter sweep's touch wake to the structural flag exactly when a region has a drain path and no `row` update stage writes a field its arm predicates read — the pending slot is written in one place while every row-set change raises the dirty bit, and `updateAt` re-runs the retained handle without moving or remounting the root, so a drain-only commit provably leaves each row's `hidden` where the last sweep put it; measured at 10k rows a non-subject row write drops 1.004 ms → 0.046 ms with the filter sweep alone (4.098 → 2.938 beside a persistence write-back that stays O(N) either way). Regions with no drain path keep the uniform flag, so every existing artifact is byte-identical; Twin Lab's new `row left mark` puts `region_touched_0` and `region_structural_0` side by side in one region, with the count sweep still waking on the drain as the boundary the next axis must move; ABI stays 17 | 2026-08-29 |
| [0083](docs/adr/0083-per-sweep-wake-flags.md) | Accepted | A region's wake flags are derived per sweep from its read sets — ADR-0082's filter rule generalizes to the counts, the emptiness/checked selections, and the persistence write-back through one predicate `sweepNarrows drainWrites reads`: a row total and an emptiness subject read only `rows.length` and can never move on a drain, a predicate count or predicate selection reads its one field, a filter table reads its arm subjects, and a write-back reads every field so it never narrows (the "never" falls out of the predicate rather than being spelled). The classification is total because the pending slot is pushed in exactly one place — the guard-miss arm of a row stage — while `append`, event and predicate `remove`, an ADR-0053 guard hit, a broadcast, and hydration all raise the dirty bit. Adjacent sweeps of one kind that agree share one block, so the flag set is derived per region: Twin Lab's `left` binds the structural flag *alone*, Mix Lab's `crew` and Toggle Lab's `items` interleave both, and drain-free regions are byte-identical. Measured at 10k rows, a Twin `mark` drops 0.0584 → 0.0371 ms (36%) where the narrowed sweeps were the region's remaining O(N) work, while Mix Lab's toggle shows no measurable change beside a persistence write-back — so the dividend is evaluation counts, not DOM, and the ADR states both columns; `{count left (flag == "true")}` joins Twin Lab as the O(N) witness and the artifact gate bans `region_touched_0`; ABI stays 17 | 2026-08-29 |
| [0084](docs/adr/0084-per-event-drain-wake.md) | Accepted | A drain wakes only the sweeps the row event it ran can move â ADR-0083 OQ1 closes one level below where the goal expected it: `listenDelegatedCells` calls *one* dispatch function per region, so a per-transaction-function write set would leave that function on the region-wide union and give every other function â, where `touched` and `structural` are already the same value (a provably empty pending slot) and the narrowing buys zero evaluations. The dispatch's own `action` argument is the witness instead: exactly one action branch runs per call, so `regions[i][3] || pending.length !== 0 && action ∈ S` is *exactly* "a drain these sweeps could see happened" â no record slot, no accumulator, one more `const` in the commit prologue. Classes are per event (an ADR-0052 key selection's arms share an action) and emitted only where they are a proper nonempty subset of the region's drain paths, so Twin Lab and Mix Lab stay byte-identical and only Toggle Lab moves (+100 bytes). Measured at 10k rows a `retype` keystroke drops 7.743 → 5.188 ms (33%, 17% at 100–1000 rows) by skipping four predicate scans and the filter sweep, while the `toggle` control is unchanged and the persistence write-back stays the O(N) floor; Toggle Lab's new editing hint gives the region a second class so the emitted disjunction is more than one action long; ABI stays 17 | 2026-08-29 |
| [0085](docs/adr/0085-per-row-serialization-cache.md) | Accepted | A persisted region's row carries its own serialization — the survey ablates rather than times (Chromium's 0.1 ms `performance.now()` clamp is larger than three of the four segments) and prices a 10k-row `retype` commit at 93.5% serialization, 5.6% `storageSet`, 0.9% key scan, 0.03% `updateAt`: so ADR-0084 OQ2's key index is not worth its invalidation matrix and OQ1's cache is worth all of it. The cache slot goes on the **row tuple**, not the region record — keyed on identity rather than position, which makes `remove`, an ADR-0050 predicate removal and an ADR-0053 guard hit provably cache-preserving (they rebuild the row array around unchanged tuples) and leaves every record slot, Mix Lab's nine/eight asymmetry included, untouched. Rows are born `null` at both construction sites and staled by exactly the two field writers ADR-0083 enumerated: the row stage, once, and the broadcast, per row. The sweep reports its encode count as `storage:{region}:encode:{n}` — the first trace whose text is computed at commit time — so a three-row lab witnesses the whole matrix without a host counter. Measured at 10k rows: `retype` 5.109 → 0.587 ms (8.7×), `toggle` 6.875 → 2.085 ms (3.3×); only the three persisting modules change and BENCHMARK.md stands; ABI stays 17 | 2026-08-29 |
| [0086](docs/adr/0086-per-row-display-cache.md) | Accepted | A filtered region's row carries its own displayed state — the same ablation survey prices a 10k-row `toggle` commit at 28.0% filter-sweep `childAt`, 23.2% `storageSet`, 21.0% filter-sweep `setProperty`, 12.1% persist push loop, 7.8% `join`, 4.7% predicate scans, 1.2% key scan and **0.0%** filter predicate evaluation: the sweep's two DOM calls are half the commit and the navigation is the larger half of that. Replacing `childAt` with a `firstChild`/`nextSibling` walk was measured (1.08×) and **rejected** — a sibling cursor must advance on every row, which is exactly the work a cache skips, and the two together (1.43×) are worse than the cache alone (3.3×). The cell goes on the row tuple behind ADR-0085's, and because its value depends on the filter *state* field as well as the row's fields it is a **write-elision** cache, not a compute cache: the predicate is recomputed for every row (the survey's 0.0%) and the cell guards only the DOM pair, so there are **zero** invalidation sites where ADR-0085 has two, and a state flip that stales every row still writes only what moved. Rows are born `null`, so a fresh or hydrated row is written exactly once; identity keying makes `remove`, a predicate removal and a guard hit write zero. The sweep reports `filter:{region}:written:{n}` and its `tx[9]` write now fires only when a row moved. Measured at 10k rows: `toggle` 2.470 → 0.763 ms (3.2×), whole-table flip +8.4% at 1000 rows and inside the noise at 10k; only the three filtering modules change and BENCHMARK.md stands; ABI stays 17 | 2026-08-29 |
| [0087](docs/adr/0087-per-commit-store-visibility.md) | Accepted | A persisted region's store is current at the end of every commit — the round's two candidates were priced on the ADR-0085/0086 ladder and both declined, and the ladder's first act was to correct ADR-0086 OQ1: measured against the shipped emission the persistence push loop is **0.040 ms**, 6.0% of a 10k-row `toggle` commit and the smallest segment, not the 0.301 ms a ladder still carrying the uncached filter sweep had attributed to it. What is left is `storageSet` at 35.5% and the `join` at 34.9%, the same shares at 1000 rows. A **per-task flush** traces `L2 + (join + storageSet)/N` exactly — 0.675 ms at N=1, 0.199 at N=400 — so it makes the flush rarer rather than cheaper, and N is 1: every listener `mount` registers dispatches one transaction, nested ones already coalesce through `tx[0]`, and the platform delivers one user event per task. A **segment array on the region record** is 1.05×/1.10× (the `join` it was meant to remove survives), and pushing it to a spliced joined string answers the O(1) question with *no* — the byte offset is a prefix sum every row edit invalidates, so the accumulation is O(position) and the 1.52×/2.30× it buys is still O(N), for two record slots and **seven** invalidation sites where identity keying owes two, with a wrong string in storage as the failure mode instead of a stale pixel. Three contract-free emission shapes (`+` accumulation, preallocated index-assign, `map`+`join`) all measured inside the noise. So the flush point becomes a **contract**: the `storageSet` runs inside the commit, synchronously, before the dispatch returns; a same-task re-read sees what that commit wrote; N dispatches in one task write N times; a tab closed at any moment loses nothing a returned dispatch wrote, so no persisting component owes an unload hook. Toggle Lab witnesses a synchronous three-click burst reading the store between clicks, and the three persisted labs' artifact gates ban the deferral primitives outright. No generated module changes and ABI stays 17 | 2026-08-29 |
| [0088](docs/adr/0088-shared-predicate-pass.md) | Accepted | A region walks its rows once per wake class — the last codegen item on the ADR-0082‥0087 line, and its own measurements inverted the round's premise twice. A Toggle Lab `toggle` commit walks the row table **six** times and three of the six spell the identical `done == "false"`. Fusing only the identical ones measures **1.155×** at 10k — *above* the 1.11× ceiling the round was handed for fusing all five — and the best rung is neither: **one pass per (region, wake class), one accumulator per distinct predicate inside it**, at **1.209×** (1.145× at 1000 rows), with `retype` flat at 1.00× on every rung because ADR-0084 keeps a keystroke out of every scan. Fusing *more* is worse: dragging the editing hint's narrower drain class into the wide one to make one pass out of five costs 0.040 ms — 40% of the whole opportunity — because a fused pass runs whenever any member is awake, so a class boundary crossed is a class boundary erased. Fusing the two post-reconcile *writer* sweeps is inside the noise and needs three code paths to keep a filter change from acquiring a storage write. So: a predicate-free total is a `length` read and joins no pass; the filter and persistence sweeps are not fusion targets; a one-member class keeps its inline loop; and what is shared is the traversal, never the cache — every slot keeps its own cell, compare, write, label and counter, and the trace, the `tx` counters, the stored string and the DOM are byte-identical. Two modules change and both get *smaller* (Toggle −4.2%, Mix −1.9%); Twin Lab is untouched. Toggle Lab witnesses the walk count directly on three rows through a counting `Symbol.iterator`: 4 for a `toggle` where it was 7, 2 for a `retype`, 3 for a `dblclick` through the narrow class's own loop. **This closes the ADR-0082‥0088 performance line.** ABI stays 17 | 2026-08-29 |
| [0089](docs/adr/0089-row-child-arity.md) | Accepted | A row template composes a *list* of children, not one — ADR-0075 OQ2 closed by retiring the `LRX-VIEW-045` arity arm with **no replacement bound and no new diagnostic number**, because the references are template-static (their count is fixed at compile time) and the second child threatens no resource the first did not. Two emissions carried the assumption and both generalize: the row root stashes `row["$lrxRowChild"] = [row_child_0, …]` in template mount order (one array literal replacing one assignment per entry, which at n > 1 silently kept only the last), and the dispose callback becomes one `for…of` loop over that stash — ADR-0075's body verbatim, so the callback is now arity-independent and a single-child region is the one-element case rather than a shape of its own. The splice touches no neighbour by *identity*, not position: a mount return is a fresh closure per instance, so two references to the same module in one row yield two distinct functions, and `indexOf` is re-read per entry so intervening shifts cannot desynchronize the run. Every other `LRX-VIEW-045` arm stands (root position, branch placement, out-of-bounds field, written-field boundary); the `RowChildTwoPerRow` fixture moves out of compile-fail and its shape moves into the lab. Mix Lab's `crew` row now carries `<Badge tag={tag}/>`, `<Stamp mark={label}/>`, `<Stamp mark="crew stamp"/>` — difference and repeat at once — while `pins` keeps one child, so one component holds a one-child and a three-child region against the same inventory; the witness removes a *middle* row and observes `[0,1,2,3,7,8,9]` survive a removal, a broadcast, and the next reconcile. Row-child-free modules stay byte-identical; no host change, ABI stays 17 | 2026-08-29 |
| [0090](docs/adr/0090-transitive-static-id-checking.md) | Accepted | The static-id rule reads a *recorded trail*, not a resolved name — ADR-0075 OQ1 (widened by ADR-0089: one row now opens as many templates as it has children) closed by extending `LRX-ELAB-135` rather than minting a code, because the rule and the repair are unchanged and only the offending attribute's location differs. Recursive resolution at the row site was rejected outright: `spec.children` carries names, and nothing puts a grandchild's `{name}_spec` in the *parent's* scope, so failing open reinstates the hole silently while failing closed rejects correct programs — either way acceptance would depend on the caller's imports. Instead `ChildComponent` gains `idTrail`, computed at the reference site inside the child's own `component` command (the one place the grandchild's spec is guaranteed to resolve, since that is what made it a `View.child` at all) and read one level up; `ComponentSpec.staticIdTrail` folds the component's own view, the row templates of every region it declares (new `RowNode.hasStaticId` — a region mints ids per row too), and every child entry's stored trail, so depth is free and the diagnostic names the path (`Cuff → Chip`). Nest Lab's roster row is promoted to `<Cuff mark={origin}/>` wrapping `<Chip tag={mark}/>`: two templates per row, the row-mount constant forwarded one level further, the per-row leaf reachable at `children[1 + i].children[0]`, and `NestLab.mjs` provably never imports `Chip.mjs` — while `Pulse` in the same child table answers `["Pulse"]` and would be rejected in row scope. The stated remaining hole: a component's *own* region row template may still carry a static `id`, left to the author and the axe gate per ADR-0071. Every generated module in every bundle byte-identical; no host change, ABI stays 17 | 2026-08-29 |
| [0091](docs/adr/0091-region-row-static-id.md) | Accepted | The static-id rule follows the *multiplication*, not the module boundary — ADR-0090's one open question closed by **moving the line** rather than restating it. ADR-0071 said the pipeline does not scout for duplicate ids and ADR-0090 narrowed that to "only templates the author is not looking at"; authorship was the wrong invariant, because the author of `<li id="row">` wrote *one* id and the region machinery is what turned it into N. The replacement: a static `id` is rejected wherever the compiler instantiates the carrying template more than once, and left to the author (and the axe `duplicate-id` gate) wherever it mounts once — from which both the row-child rejection (`LRX-ELAB-135`) and the new own-template one re-derive, and nothing else in the language multiplies a template. The rejection lands at the **model layer**, one `if` in `validateRegions` whose decision is `RowNode.hasStaticId` — the same walk `ComponentSpec.staticIdTrail` folds, so "mints ids per row" and "is rejected" cannot drift apart, and the fact holds for hand-written specs that never pass through the `component` command; the elaborator's exact `id=` span was traded for the region declaration's, which is the granularity the message is about anyway and is pinned in the compile-fail gate. A new number `LRX-VIEW-046`, not a third `LRX-ELAB-135` branch, because the rejected construct, the layer, and the message's subject all differ — 135 has no child to name here. ADR-0090's option (c) is **declined, not deferred**: matching a `RowNode.child` name back to the child table would trade an exact reference-site span for a lookup obligation and buy no new rejection. Witnesses: the `RegionRowStaticId` fixture (id in the row template, `id` on the view's `<ul>` untouched, both sides in one file) and forged-spec assertions at the row root, one cell down, and inside a branch subtree, plus the accepting pair — a view id passes `check` while still answering `["Roster"]` for a composer. All 155 generated files across nineteen generators byte-identical, `graphHash` included; no host change, ABI stays 17 | 2026-08-29 |
| [0092](docs/adr/0092-key-ordered-row-tables.md) | Accepted | A key resolves to its position by *search*, not by index — the open question ADR-0084 raised and 0085‥0088 each copied forward unmoved, closed by rejecting its own premise. The proposal was a key→index map on the region record in exchange for a seven-site invalidation matrix; the premise was that skipping the walk requires *remembering* where a key lives. It does not: a row table is already **strictly ascending in `row[0]`**, and for reasons all sealed elsewhere — both `push` sites take `regions[r][2]` and increment it, no emission ever assigns a row's key slot again, every removal is order-preserving, and the backend has no swap, sort, or insert-at. So the invalidation matrix is not a price but a *theorem*, one row per column, every cell empty. Re-measured paired (every rung at each load index, generated Toggle Lab hydrated through the real storage path, 400 dispatches per sample): the search beats the emitted scan on every workload — `retype` 0.5430 → 0.5088 ms at 10k (1.067×), `toggle` 0.6313 → 0.6095 (1.036×) — and the map *ties* it on every read path while running **5.4% slower than doing nothing** on a remove-and-append churn at 10k, because a removal shifts every later position. ADR-0085's 1.5×-against-seven-sites is superseded by that table. The two single-key removals now `splice` at the resolved position (a guard hit reuses the one its own stage resolved); `removeIf` keeps its kept-filter, `O(N)` by contract. The search is *generated*, one `$lrx_row_seek(rows, key)` per module shared by every region — not a host export, which is what keeps ABI at 17 and every manifest byte-identical — and folding it out of the six inline copies makes Toggle Lab 692 bytes *smaller*. The emitter gains one AST node, `Stmt.whileLoop`, binding nothing and used in one place, with a `(span - span % 2) / 2` midpoint that is exact at every array length. Toggle Lab witnesses nine matrix cells including append-after-removal and a sweep in the same commit as a drain; walks per commit fall 4→3 on a `toggle`, 2→1 on a `retype`, 5→4 on a ✕. Five modules change, none by more than 0.4%; every `.graph.json`, every manifest and the benchmark bundle byte-identical | 2026-08-29 |
| [0093](docs/adr/0093-machine-checked-row-order.md) | Accepted | The key order ADR-0092's search depends on is checked by the compiler — ADR-0092's own first open question, judged by *paying* it. That ADR declined a structural check because it "would restate the enumeration above in a second place"; the enumeration is a table with **one row per emission site** and grows with the backend, while the check is a list with **one rule per thing that can happen to a JavaScript array** and does not — JavaScript has no seventh way to disturb an array's order, so a tenth emission site is covered by rules written before it existed. The layer is an **audit over the emitted module**, run inside `Backend.Component.emit` right after `Module.validate`, because every path's statements land in that one `Js.Module`: there is nothing to opt into. Narrowing the emission helpers was declined as *uncheckable* — `Stmt.assign` and `Expr.call` cannot be taken away, so the paths that break the order are exactly the ones that skipped the vocabulary. Eight rules under one new code, **`LRX-BE-036`**, whose message names the rule and the function: R0 every row table is spelled `regions[r][1]` (no double-indexed context, no second name for the record) — which is what makes the audit's reach a fact; R1 a table only occupies approved positions (no alias, no `sort`, no `unshift`); R2 a `push` takes one row literal headed by `regions[r][2]`; R3 a `splice` removes exactly one row; R4 no key is overwritten, through a row binding, an element read, *any* parameter, or a record replacement; R5 a whole-table assignment is the ADR-0050 kept filter and nothing else; R6 the counter only advances; R7 a region mounts empty. Cost **718 µs** for Toggle Lab compiled — below the generator's own measurement floor — and **zero output bytes**: all five region-carrying bundles byte-identical under the stash protocol, ABI stays 17. Witness: twenty-two order-breaking statements injected into the *real* emitted module (insert-at, sort, swap in three spellings, borrowed key, rewound counter) each rejected under their named rule, plus two acceptances so R5 is a shape and not a ban; the emitter broken twice end-to-end, and the audit broken twice — including the vacuity mode, where a blinded table recogniser makes the real emission fail R5 rather than pass quietly | 2026-08-29 |
| [0094](docs/adr/0094-host-side-order-contract.md) | Accepted | The host does not disturb the array it is handed — ADR-0093 OQ1, judged by *paying* it. The emission side is audited and the call boundary was not: the backend hands `regions[r][1]` to `update`, one of its rows to `updateAt`, and the table again to `$lrx_row_seek`, while `runtime/leanrx_region.mjs` holds a `splice`, a two-slot exchange and a positional removal — each correct on the host's own entry array and catastrophic on the caller's, and identical in the source, because only what the identifier is *bound to* distinguishes `current.splice` from `items.splice`. A rule over the host source is therefore declined as uncheckable on the same ground ADR-0093 declined a narrowed emission vocabulary: answering it statically needs a JavaScript AST and a binding analysis, where the emitter has a Lean AST only because it builds one; an ABI sentence plus a bump is declined because a bump says the host *changed*, not that the change kept the order — it is the review being replaced. The layer is a **contract test** at `check_region_runtime.sh`, four rules under one code **`LRX-HOST-001`**: H1 every caller array crosses as a frozen copy and stays registered, so an ordinary mutation throws inside the host call and a stashed alias throws three calls later; H2 every row's key slot is snapshotted and re-verified (rows cross *unfrozen* — a host forwards them to callbacks that own the ADR-0085/0086 cache slots, and only slot 0 is the order's business); H3 the handle surface is closed, so a new export — the event that already moves `runtimeAbi` — must declare its caller arrays before the gate is green; H4 every array-taking entry point must actually be exercised, so a neutered guard cannot pass quietly. That closed surface is the asymmetry with ADR-0093's rejection of a running witness: the emission surface is open and grows silently, the host surface is pinned to an ABI event. The one bypass a freeze does not throw on — `Reflect.set`, which fails *silently* on a frozen array and would mutate the unfrozen one production passes — is banned by one regex beside the existing `new Proxy`/`eval(`/`innerHTML` ban. Wiring is three lines, so all fifteen region constructions and both fuzz loops (400 placement rounds, 600 monotone-key rounds over numbers, bigints, strings, symbols, objects and mixed types) run behind the guard, plus one case playing **both caller shapes**: the component backend's monotone-counter table, sealed single-row `splice` and append-past-the-hole, asserted ascending and re-searched by a mirror of the generated `$lrx_row_seek` after every host call, and the benchmark's own array through `swapAt` and `removeAt`. Witness: the host broken six ways (reversed table, stashed table spliced three calls later, an equal-keyed row replacement no other assertion can see, a re-keyed row, an exchanged pair, an unclassified new export) each red under its named rule, and the guard broken twice — deleting H1 makes the equal-keyed replacement pass, and neutering the wrapper fails H4 naming all six unexercised entry points. Cost 0.116 → 0.415 s in one of twenty gates; nothing under `runtime/` or `LeanRx/` changes, so every generated module and every bundled host file is byte-identical and ABI stays 17 | 2026-08-29 |
| [0095](docs/adr/0095-row-tables-across-call-boundaries.md) | Accepted | A row table stays a row table when it becomes a parameter — ADR-0094 OQ1, paid by widening R0 rather than by adding a rule. ADR-0093's R1 lets a table become an argument, and inside the callee it is a parameter name, so seven of the eight rules lose their subject: `rows.sort()`, `rows.splice(i, 2)`, `rows.unshift(row)`, `const t = rows`, `return rows`, `rows[i] = rows[j]` and `rows[i][0] = k` passed every gate the repo has, confirmed by injection — with `rows["sort"]()` as `$lrx_row_seek`'s first statement the pre-change emitter puts the sort in Toggle Lab's bundle and the generator exits zero, and a natural sort of row tuples reorders a table the moment a region has minted ten keys, inside the binary search that requires the order. The three gates that look like they cover it do not: the codegen double-generate `diff -ru` proves determinism and a sorting helper is emitted identically twice; the artifact tests pin whatever the emitter currently produces; `LRX-BE-018` is a scoping rule and `rows["sort"]()` is well-scoped. Only `rows[0] = …` was caught, by R4's parameter half, and by coincidence. **R0 now names two spellings** — `regions[r][1]`, and a parameter of a module function that some call site hands one to — so there is **no ninth rule and no new code**: rejections stay `LRX-BE-036` and only the subject half of the message is new. The propagation is a least fixpoint over `(function, argument position)` pairs, not one step, so a second helper inherits the rules instead of renegotiating them; the budget is the module's total parameter count and `audit` re-sweeps and rejects under R0 if the set is not stable, so it fails closed. R2 and R5 are *unsatisfiable* at a parameter — no counter to name, no region slot to install into — which is the existing rules evaluated at a region-less subject, not a new ban; R3 survives on merit, a single-row `splice` being order-preserving whichever table it is. Witness 24 -> 43 cases over the real Toggle Lab module: sixteen new rejections, every one accepted before this round, plus three new acceptances, four of the nineteen watching the fixpoint cross two calls into an appended second helper and one pinning the rule to call sites rather than to the name `rows`. Emitter broken three ways — the sort, a two-row splice, and a forwarding helper `$lrx_row_trim` that sorts what it is handed — each red under its named rule at `lake exe leanrx_toggle_js`; audit broken four ways, the two rule deletions turning the witness red and both vacuity modes failing the *real* emission rather than passing quietly. Cost 666 → 1 483 µs per audit of the largest module, three extra traversals, against a 0.54–0.59 s generator; zero output bytes, five region labs byte-identical under `git stash` regeneration, ABI stays 17 | 2026-08-29 |

| [0096](docs/adr/0096-persisted-payload-cost-model.md) | Accepted | A persisted region's write costs its bytes and its join costs its rows — ADR-0087 OQ1, closed by measuring what the two segments are functions *of* rather than only how big they are. The ladder re-derived against the post-0088/0092 emission reads 37.7% `storageSet` / 40.5% `join` on a 10k `toggle`, and two new rows say what ADR-0087's two could not: a `retype` commit is **99.8%** persistence (ADR-0082's drain wake leaves the predicate pass, the filter sweep and the hidden scan all asleep for a keystroke, and the write-back is the one sweep that cannot narrow), while a single-row structural commit is 5.7-5.9 ms of which the floor is at most 10%. Two cells carrying nearly the same payload in 1 000 and 10 000 rows separate the segments outright: `storageSet` moves 0.2115→0.2335 and the `join` moves 0.0380→0.2508. Probed directly, `setItem` is **5.0 µs + 0.85 ns/byte with no row term** (predicting the ladder out of sample at 100, 1 000 and 10 000 rows) and `join` is **~18 ns/row + ~0.1 ns/byte** (53× spread for a 1.2× byte spread at fixed payload); they cross at ~23 bytes per row and a Toggle Lab row is 24.8, so "35% each" was a coincidence, confirmed from the other side by a 223-byte row landing at 5.6 : 1. The ABI exit was then **built** three ways — the host joining the segment array (0.99×-1.01×), the host roping the caller's row table (1.03×-1.10×), the host arraying and joining it (0.98×-1.03×), all twelve cells byte-identical in the store — so it wins nothing worth freezing the row tuple's slot layout into the ABI, and chunked keys are declined on ADR-0087's visibility sentence rather than on cost (`localStorage` has no multi-key transaction, so another tab can see one chunk and not the next; IndexedDB withdraws the same sentence harder). What lands is a **cost contract** in `PersistSpec` and the guide: the payload is the component's own bytes, one separator between each pair of a row's declared fields and one row separator between rows and nothing else, so the levers are narrower rows and fewer rows and neither is the compiler's to pull. Witnesses pin the two structural facts the model rests on — Toggle Lab's no-framing-tax length identity over labels carrying all three escapes, and one key for the origin; Mix Lab's exactly-two-keys, one whole table each — broken three ways — a per-row position index (red on the identity), a second chunk key beside the declared one and a chunked second region on the two-region component (both red on the key set alone, where the declared values still read correct and no existing assertion sees them). No generated module changes and ABI stays 17. OQ1 is replaced by the host's full-table reconcile: a one-row `remove` re-runs the generated per-row update callback on every retained row, 4.95 ms against a `toggle`'s 0.097 ms, and `removeAt` has been in the host since ADR-0026 unused | 2026-08-29 |

The governing decision process is described in `ARCHITECTURE.md` and `PLAN.md`.
New accepted ADRs may refine those documents only when the rationale, migration
impact, contract updates, and tests are committed together.
